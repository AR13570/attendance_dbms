import 'package:flutter/material.dart';

class GradeHome extends StatelessWidget {
  const GradeHome({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("Choose subject")],
      ),
    );
  }
}
